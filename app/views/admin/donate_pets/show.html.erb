<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-primary text-white text-center py-1">
          <h3 class="mb-0">Donation Request Details</h3>
        </div>

        <div class="card-body p-4">
          <div class="row mb-3">
            <div class="col-md-6">
              <p class="fw-bold">Donation ID:</p>
              <p class="text-muted"><%= @donate_pet.id %></p>
            </div>
            <div class="col-md-6">
              <p class="fw-bold">Status:</p>
              <% status_class = @donate_pet.pending? ? 'badge bg-warning' : (@donate_pet.accepted? ? 'badge bg-success' : 'badge bg-danger') %>
              <span class="<%= status_class %> px-3 py-2"><%= @donate_pet.status.capitalize %></span>
            </div>
          </div>

          <hr>

          <h5 class="text-primary mb-3">User Details</h5>
          <div class="row">
            <div class="col-md-6">
              <p><strong>Name:</strong> <%= @donate_pet.user.first_name %> <%= @donate_pet.user.last_name %></p>
              <p><strong>Email:</strong> <%= @donate_pet.user.email %></p>
              <p><strong>Phone No:</strong> <%= @donate_pet.phone_no %></p>
            </div>
            <div class="col-md-6">
              <p><strong>Address:</strong> <%= @donate_pet.address %></p>
            </div>
          </div>

          <hr>

          <h5 class="text-primary mb-3">Pet Details</h5>
          <div class="row">
            <div class="col-md-6">
              <p><strong>Category:</strong> <%= @donate_pet.category_name %></p>
              <p><strong>Breed:</strong> <%= @donate_pet.breed_name %></p>
              <p><strong>Gender:</strong> <%= @donate_pet.pet_gender %></p>
            </div>
            <div class="col-md-6">
              <p><strong>Expected Donation Date:</strong> <%= @donate_pet.expected_donate_date.strftime("%d-%m-%Y") if @donate_pet.expected_donate_date %></p>
              <p><strong>Actual Donation Date:</strong> <%= @donate_pet.actual_donate_date.strftime("%d-%m-%Y") if @donate_pet.actual_donate_date %></p>
            </div>
          </div>

           <!-- Carousel for Pet Images -->
        <% if @donate_pet.pet.pet_images.attached? %>
          <div id="petImageCarousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
              <% @donate_pet.pet.pet_images.each_with_index do |image, index| %>
                <div class="carousel-item <%= 'active' if index == 0 %>">
                  <%= image_tag image, class: "d-block w-100", style: "height: 300px; object-fit: cover;" %>
                </div>
              <% end %>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#petImageCarousel" data-bs-slide="prev">
              <span class="carousel-control-prev-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#petImageCarousel" data-bs-slide="next">
              <span class="carousel-control-next-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Next</span>
            </button>
          </div>
        <% else %>
          <div class="text-center p-3">
            <p class="text-muted">No images available</p>
          </div>
        <% end %>

        </div>

        <div class="card-footer text-center bg-light">
          <%= link_to "Back to Requests", admin_donate_pets_path, class: "btn btn-secondary px-4 py-2" %>
        </div>
      </div>
    </div>
  </div>
</div>
